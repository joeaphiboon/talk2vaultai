import React from 'react';

const InstallPrompt: React.FC = () => {
  // No installation prompts - users can only install by manually adding to home screen
  return null;
};

export default InstallPrompt;